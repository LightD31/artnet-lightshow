/*
 * Upgrade scripts run once per connection to migrate saved config, actions
 * and feedbacks when their format changes between module versions.
 * Remember that once a script has been added it cannot be removed!
 */
export const UpgradeScripts = []
